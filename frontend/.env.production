# Do not set VITE_API_URL here for EC2 / same-origin Nginx deploys (axios uses relative /api, /token).
# Netlify sets VITE_API_URL in netlify.toml [build.environment] for Netlify + Railway API.
# Railway static builds: set VITE_API_URL in the Railway service Variables if needed.
